<%= form_for(@show_item) do |f| %>
  <% if @show_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@show_item.errors.count, "error") %> prohibited this show_item from being saved:</h2>

      <ul>
      <% @show_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
