<style>
  .vcenter {
    vertical-align: middle;
  }

  .col-align {
    display: inline-block;
  }
  
  .col-align--top {
    vertical-align: top;
  }

  .col-align--center {
    vertical-align: middle;
  }
  
  .col-align--bottom {
    vertical-align: bottom;
  }
  
  .v-center {
    position: relative;
    top: 50%;
    -webkit-transform: translateY(-50%);
    -ms-transform: translateY(-50%);
    transform: translateY(-50%);
  }

  input,
  textarea {
  border: 1px solid #eeeeee;
  box-sizing: border-box;
  margin: 0;
  outline: none;
  padding: 10px;
  }

  input[type="button"] {
  -webkit-appearance: button;
  cursor: pointer;
  }

  input::-webkit-outer-spin-button,
  input::-webkit-inner-spin-button {
  -webkit-appearance: none;
  }

  .input-group {
  clear: both;
  margin: 15px 0;
  position: relative;
  }

  .input-group input[type='button'] {
  background-color: #eeeeee;
  min-width: 38px;
  width: auto;
  transition: all 300ms ease;
  }

  .input-group .button-minus,
  .input-group .button-plus {
  font-weight: bold;
  height: 38px;
  padding: 0;
  width: 38px;
  position: relative;
  }

  .input-group .quantity-field {
  position: relative;
  height: 38px;
  left: -6px;
  text-align: center;
  width: 62px;
  display: inline-block;
  resize: vertical;
  }

  .button-plus {
  left: -13px;
  }

  input[type="number"] {
  -moz-appearance: textfield;
  -webkit-appearance: none;
  }

</style>

<script>
  function incrementValue(e) {
    e.preventDefault();
    var fieldName = $(e.target).data('field');
    var parent = $(e.target).closest('div');
    var currentVal = parseInt(parent.find('input[name=' + fieldName + ']').val(), 10);

    if (!isNaN(currentVal)) {
      parent.find('input[name=' + fieldName + ']').val(currentVal + 1);
    } else {
      parent.find('input[name=' + fieldName + ']').val(0);
    }
  }

  function decrementValue(e) {
    e.preventDefault();
    var fieldName = $(e.target).data('field');
    var parent = $(e.target).closest('div');
    var currentVal = parseInt(parent.find('input[name=' + fieldName + ']').val(), 10);

    if (!isNaN(currentVal) && currentVal > 0) {
       parent.find('input[name=' + fieldName + ']').val(currentVal - 1);
    } else {
       parent.find('input[name=' + fieldName + ']').val(0);
    }
  }

  $(document).ready(function() { 
    $('.input-group').on('click', '.button-plus', function(e) {
       incrementValue(e);
    });

    $('.input-group').on('click', '.button-minus', function(e) {
      decrementValue(e);
    });
  });
  
</script>

<div class="panel panel-default">
  <div class="panel-body">
    <h1>
      <STRONG>Team Ides - Badge Sales</strong>
    </h1>

    <hr/>

    <p>
      Thanks for stopping by. We're using troupeIT to process sales
      for our unofficial DEF CON 27 badge.
      <br/>
      No sign up is necessary to purchase a badge.
    </p>

    <h3>
      Step 1: Select Items and Quantity
    </h3>

    <hr/>
    
    <div class="row col-align col-align--top">
      <div class="col-md-12 col-lg-12">
      <div class="col-md-3 col-lg-3  col-align col-align--top">
        <img src="<%=asset_path 'badge/bomb1.jpg'%>" width="200" alt="dabomb image"/>
      </div><!--
      --><div class="col-md-3 col-lg-3 col-align col-align--top">
        <font size="+1">
          <strong>DC27 Unofficial Badge: da Bomb</strong>
        </font>

        <p>A limited edition, wearable, hackable, electronic
        conference badge with blinky RGB lights, display, and stereo
        sound.
        </p>
        
        <p>
          Designed by <a href="https://ides.team/">Team Ides</a>,
          Ships from United States of America
        </p>
      </div><!--
     --><div class="col-md-2 col-lg-2 text-center vcenter">
        <P>
          <BR/>
          <font size="+2">$120.00</font>
        </p>
        </div><!--
      --><div class="col-md-2 col-lg-2 text-center vcenter"><!--
          --><div class="input-group">
            <input type="button" value="-" class="button-minus" data-field="quantity">
            <input type="number" step="1" max="" value="1" name="quantity" class="quantity-field">
            <input type="button" value="+" class="button-plus" data-field="quantity">
          </div>
        </div><!--
       --><div class="col-md-2 col-lg-2 text-center vcenter"><!--
       --><div id="qtytot">
          <P>
            <BR/>
            <font size="+2">$120.00</font>
          </P>
        </div>
      </div>
      </div>
    </div>
    <hr/>

    <h3>
      Step 2: Check out.
    </h3>
    
    <font size="-1">
      <I>
        Payments will be processed securely with stripe.com and will
        show up as 'troupeIT - defcon27' on your statement.
      </I>
    </font>
  </div>
</div>
